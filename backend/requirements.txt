# Local dev deps (uvicorn/pytest). Runtime pins must stay in sync with the
# root requirements.txt used by the Vercel function build.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic-settings>=2.4
pytest>=8.0
pytest-asyncio>=0.24
